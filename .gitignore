.terraform
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
# Ignore override files as they are usually used to override resources locally and so contain sensitive data
override.tf
override.tf.json
# Include override files you do wish to add to version control using negated pattern
!override_example.tf
!override_example.tf.json
# Ignore CLI configuration files containing credentials
.terraformrc
terraform.rc
terraform.tfvars
# Ignore tfvars files, which are likely to contain sensitive data, such as passwords or API keys